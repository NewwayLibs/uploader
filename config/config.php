<?php

return array(

    /*
    |--------------------------------------------------------------------------
    | Uploader config
    |--------------------------------------------------------------------------
    */

        'uploader' => [
            /*
            |--------------------------------------------------------------------------
            | Root path of stored files
            |--------------------------------------------------------------------------
            */
                'path'                    => 'uploads/images',
            /*
            |--------------------------------------------------------------------------
            | Scheme of directory path generator                |
            |--------------------------------------------------------------------------
            | Supported:
            | "linear" (path/module/filename.png)
            | "hash" (path/module/fi/le/filehash.png)
            */
                'scheme'                  => 'hash',
            /*
            |--------------------------------------------------------------------------
            | Hash levels
            |--------------------------------------------------------------------------
            | Array of hash levels length
            */
                'hash_levels' => [2,2],

        ],
    /*
    |--------------------------------------------------------------------------
    | Image
    |--------------------------------------------------------------------------
    */

        'auth'     => [
                'login'    => env('SPEED_SMS_LOGIN'),
                'password' => env('SPEED_SMS_PASSWORD'),
        ],


);